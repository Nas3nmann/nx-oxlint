{
  "name": "react-app",
  "targets": {
    "oxlint": {
      "executor": "nx-oxlint:lint",
      "cache": true,
      "inputs": ["default", "^default"],
      "options": {
        "projectRoot": "apps/react-app"
      }
    }
  }
}
