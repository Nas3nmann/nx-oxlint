{
  "name": "ts-lib",
  "targets": {
    "oxlint": {
      "executor": "nx-oxlint:lint",
      "cache": true,
      "inputs": ["default", "^default"],
      "options": {
        "projectRoot": "libs/ts-lib"
      }
    }
  }
}
